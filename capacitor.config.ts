import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'real-time-chat',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
